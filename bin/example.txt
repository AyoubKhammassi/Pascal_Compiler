program test;
var a,b,c : real;

(*Function example*)
function func1(a:integer) : integer
var d : real;
begin
end

function func2(a:real) : integer
var b;
begin
end

begin

(*If statement example*)
if((a==b)&&(b <= c))
then
begin

end

(*Different operators example*)

a:=b*c;
d++;

end